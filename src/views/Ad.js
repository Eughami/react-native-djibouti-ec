import { Text, View } from 'react-native'

function Ad() {
  // TODO. Add view page title, slider(image+video), description, price, call+whastapp, time, views
  // TODO. Add a view with device details (on mount ??????--on scroll--?????)
  return (
    <View style={{ flex: 1 }}>
      <Text style={{ fontWeight: 'bold', fontSize: 40, color: 'white' }}>
        Hello MF
      </Text>
    </View>
  )
}

export default Ad
